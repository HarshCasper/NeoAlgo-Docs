---
title: Harshad Number
slug: /mathematics/harshad-number
---

## Introduction

A number is called a Harshad number if it is divisible by the sum of its digits. It can also be referred to as Niven number.

## Explanation

If the number under consideration is taken and the sum of its individual digits is calculated, if this resultant sum is divisible by the number itself then the number will be considered a Harshad number.

Let's have one example: 
```
Input number : 120
Output       : Is a Harshad Number

Sequential steps :
1 + 2 + 0 = 3
120 % 3 = 0
```

## Algorithm

- Loop through the individual digits of the number and add it to the `sum` variable.
- Now check if the number is divisible by `sum`.
- If it is then we can conclude that it is a Harshad number or else it is not.

## Code

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs
  defaultValue="py"
  values={[
    { label: 'Python', value: 'py', },
    { label: 'Dart', value: 'dart', },
  ]
}>
<TabItem value="py">

```py

def isHarshad(number) :
    copy = number

    # Calculating the sum of digits of the number
    sum_of_digits = 0
    while number != 0 :
        digit = number % 10
        sum_of_digits += digit
        number = number // 10

    # Checking if Number is divisible by sum of digits
    if copy % sum_of_digits == 0 :
        return True
    return False


user_input = int(input("Enter a number:"))

if isHarshad(user_input):
    print(user_input, "is a Harshad number")
else:
    print(user_input, "is not a Harshad number")

```

</TabItem>
<TabItem value="dart">

```dart
import 'dart:io';

// Check if a number is a Harshad number
bool isHarshad(int number) {
  int sum = 0;
  int copy = number;

  // Seperate individual digits and add to sum
  while (number > 0) {
    int digit = number % 10;
    number ~/= 10;
    sum += digit;
  }

  // Check if number is divisible by sum
  if (copy % sum == 0) {
    return true;
  }
  return false;
}

// Main Function, with driver code
void main() {
  print("Enter a number:");
  int input = int.parse(stdin.readLineSync()!);

  // Call isHarshad function
  if (isHarshad(input)) {
    print("$input is a Harshad Number");
  } else {
    print("$input is not a Harshad Number");
  }
}
```

</TabItem>
</Tabs>

## Sample Input and Output

### Input:

```
Enter a number : 84
```

### Output:

```
84 is a Harshad number
```

## Complexity Analysis

- Time  complexity - ` O(log(n)) `
- Space complexity - ` O(1) `

## Credits

- [Ayush Jain](https://github.com/Ayushjain2205) contributed the [Python implementation](https://github.com/TesseractCoding/NeoAlgo/blob/master/Python/math/HarshadNumber.py)
- [Ayush Jain](https://github.com/Ayushjain2205) contributed the [Dart implementation](https://github.com/TesseractCoding/NeoAlgo/blob/master/Dart/maths/HarshadNumber.dart)
