---
title: Harshad Number
slug: /mathematics/harshad-number
---

## Introduction

A number is called a Harshad number if it is divisible by the sum of its digits. It can also be referred to as Niven number.

## Explanation

If the number under consideration is taken and the sum of its individual digits is calculated, if this resultant sum is divisible
by the number itself then the number will be considered a Harshad number.

Let's have one example: 
```
Input number : 120
Output       : Is a Harshad Number

Sequential steps :
1 + 2 + 0 = 3
120 % 3 = 0
```

## Algorithm

- Loop through the individual digits of the number and add it to the `sum` variable.
- Now check if the number is divisible by `sum`.
- If it is then we can conclude that it is a Harshad number or else it is not.

## Code

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs
  defaultValue="cpp"
  values={[
    { label: 'C++', value: 'cpp', },
    { label: 'Python', value: 'py', },
  ]
}>
<TabItem value="cpp">

```cpp
#include <bits/stdc++.h>
using namespace std;

// Function to check whether the Number is Harshad Number or Not.

bool isHarshad(int number)
{
    int sum = 0;
    int copy = number;
    while (number > 0)
    {
        int digit = number % 10;
        number = number / 10;
        sum += digit;
    }
    return copy % sum == 0;
}

int main()
{
    cout << "Enter a Number:" << endl;
    int input;
    cin >> input;
    if (isHarshad(input))
        cout << input << " is a Harshad Number." << endl;
    else
        cout << input << " is not a Harshad Number." << endl;
    return 0;
}

```

</TabItem>
<TabItem value="py">

```py

def isHarshad(number) :
    copy = number

    # Calculating the sum of digits of the number
    sum_of_digits = 0
    while number != 0 :
        digit = number % 10
        sum_of_digits += digit
        number = number // 10

    # Checking if Number is divisible by sum of digits
    if copy % sum_of_digits == 0 :
        return True
    return False


user_input = int(input("Enter a number:"))

if isHarshad(user_input):
    print(user_input, "is a Harshad number")
else:
    print(user_input, "is not a Harshad number")

```

</TabItem>
</Tabs>

## Sample Input and Output

### Input:

```
Enter a number : 84
```

### Output:

```
84 is a Harshad number
```

## Complexity Analysis

- Time  complexity - ` O(log(n)) `
- Space complexity - ` O(1) `

## Credits

- [Ayush Jain](https://github.com/Ayushjain2205) contributed the [Python implementation](https://github.com/TesseractCoding/NeoAlgo/blob/master/Python/math/HarshadNumber.py)
- [Ayush Jain](https://github.com/Ayushjain2205) contributed the [C++ implementation](https://github.com/TesseractCoding/NeoAlgo/blob/master/C-Plus-Plus/math/HarshadNumber.cpp)
