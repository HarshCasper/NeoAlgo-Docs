---
title: Ackermann Function
slug: /mathematics/Ackermann-Function
---

## Introduction

The Ackermann function is one of the classic example of a recursive function, that is not a primitive recursive function. It grows very quickly in value, faster than an exponential function.

## Explanation

It is basically a function with two non-negative integers as arguments.
Assuming `i` and `j` as the non-negative integers.
Ackermann function can be declared as:
```
A(0, j)=j+1 for j ≥ 0
A(i, 0)=A(i-1, 1) for i > 0
A(i, j)=A(i-1, A(i, j-1)) for i, j > 0
```
Let's have few examples: 
```
For i=1,j=2
Output=4

For i=3,j=3
Output=61
```

## Algorithm

```
- Taking two non-negative integers from user.
- Calling the function with the input values.
Ackermann_Function(int i, int j):
  - Checking the values of `i` and `j`, 
    then applying the associated conditions.
```

## Code

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs
  defaultValue="cpp"
  values={[
    { label: 'C++', value: 'cpp', },
    { label: 'Dart', value: 'dart', },
  ]
}>

<TabItem value="cpp">

```cpp
#include<bits/stdc++.h>
using namespace std;
int Ackermann_Function(int m, int n) {
	if (m == 0)
		return n + 1;

	else if (m > 0 && n == 0)
		return Ackermann_Function(m - 1, 1);

	else if (m > 0 && n > 0)
		return Ackermann_Function(m - 1, Ackermann_Function(m, n - 1));
}

int main() {
	int m, n;
	cout << "Input first Number\n";
	cin >> m;
	cout << "Input second Number\n";
	cin >> n;
	cout << "Result: " << Ackermann_Function(m, n);
	return 0;
}
```

</TabItem>
<TabItem value="dart">

```dart
import 'dart:io';

int Ackermann_Function(int m, int n) {
  if (m == 0)
    return n + 1;
  else if (m > 0 && n == 0)
    return Ackermann_Function(m - 1, 1);
  else if (m > 0 && n > 0) return Ackermann_Function(m - 1, Ackermann_Function(m, n - 1));

  return 0;
}

void main() {
  print("Input first Number");
  int m = int.parse(stdin.readLineSync()!);
  print("Input second Number");
  int n = int.parse(stdin.readLineSync()!);
  int result = Ackermann_Function(m, n);
  print("Result: $result");
}
```

</TabItem>
</Tabs>

## Sample Input and Output

#### Input:

```
Input first Number
1
Input second Number
2
```

#### Output:

```
Result: 4
```

## Complexity Analysis

- Time  complexity - `O(mA(m, n))` to calculate `A(m,n)`
- Space complexity - `O(m)` to calculate `A(m, n)`
  Here, `m` and `n` are the two integers that we take from user

## Credits

- [Ritvij Kumar Sharma](https://github.com/ritvij14) contributed the [Dart implementation](https://github.com/TesseractCoding/NeoAlgo/blob/master/Dart/maths/Ackermann_function.dart)
- [Yash Thakare](https://github.com/YashThakare) contributed the [C++ implementation](https://github.com/TesseractCoding/NeoAlgo/blob/master/C-Plus-Plus/math/Ackermann_function.cpp)
